{
    "development": {
        "config": {
            "port": 3000,
            "configProvider": { 
                "module": "./config_providers/json-local",
                "source": "./someConfig.json"
            },
            "auth": "./basicAuth",
            "routes": "./router"
        }
    },
    "production": {
        "config": {
            "port": 80
        },
        "extend": "development"
    }
}