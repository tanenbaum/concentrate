{
    "development": {
        "config": {
            "port": 3000,
            "configProvider": { 
                "module": "json-local"
            },
            "auth": "./basicAuth"
        }
    },
    "production": {
        "config": {
            "port": 80
        },
        "extend": "development"
    }
}